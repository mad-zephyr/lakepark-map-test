import { FC } from 'react'

import styles from './styles.module.sass'
import { Separator } from '../components/separator/separator'

export const Sections02: FC = () => {
  return (
    <section className={styles.sections}>
      <Separator />
      Sections02
    </section>
  )
}
